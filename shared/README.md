# ChinaBound shared assets

本目录是 ChinaBound OpenClaw 工程的共享层，供全部 agents 读取。

- `schemas/`: 跨 agent 的统一 JSON Schema。
- `workflows/`: 共享自动化流程 playbook。
- `tasks/`: 示例触发任务和任务包。
- `data/`: 样例白名单、渠道映射、风险矩阵和内容分类。
