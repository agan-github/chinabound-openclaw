# 按时间窗口定时生成

1. `time-trigger` 根据 cron 或节庆规则发起任务。
2. `time-tagging` 过滤有效窗口内的事件与内容。
3. `assignment-planner` 确认优先级和体裁。
4. `newsletter-editor`、`destination-story`、`utility-guide` 并行产出。
5. `channel-packaging` 生成发布版本。
6. `website-publishing`、`social-distribution`、`messaging` 发布。
