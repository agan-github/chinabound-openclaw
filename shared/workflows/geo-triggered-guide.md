# 按地理位置自动生成

1. `geo-trigger` 接收位置事件。
2. `geo-tagging` 汇总附近候选对象。
3. `itinerary-composer` 生成半日/一日路线。
4. `utility-guide` 生成实用提醒。
5. `channel-packaging` 产出 App、消息和礼宾版本。
6. `messaging` 或 `app-mini-program` 触达用户。
