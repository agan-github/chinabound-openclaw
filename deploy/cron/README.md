# Cron install pack

OpenClaw cron jobs are persisted by the Gateway under `~/.openclaw/cron/` and are normally created with CLI commands rather than stored as static config.

This folder provides install-ready shell commands and editable manifest JSON files for ChinaBound's recurring jobs.
